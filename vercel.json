{
  "buildCommand": "cd arbitrage-landing-page && npm install && npm run build",
  "outputDirectory": "arbitrage-landing-page/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
